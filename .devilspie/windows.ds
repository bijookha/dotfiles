(if (or
      (matches (window_class) "MPlayer")
      (matches (window_class) "vlc")
      (matches (window_class) "Firefox")
      (matches (window_class) "URxvt")
    )
  (begin
    (undecorate)
  )
)
